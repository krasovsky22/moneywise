pre-commit:
  parallel: true
  commands:
    python-lint:
      glob: "apps/api/**/*.py"
      root: "apps/api"
      run: uv run ruff check {staged_files} && uv run ruff format {staged_files}
    js-lint:
      glob: "apps/web/**/*.{ts,tsx}"
      run: pnpm --filter web lint
