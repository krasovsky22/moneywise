# Copy to .env.local for local overrides (gitignored)
# VITE_API_URL is optional in dev — the Vite proxy handles /api/* → http://localhost:8000
# Set it in production to your deployed API URL
# VITE_API_URL=https://api.moneywise.example.com
